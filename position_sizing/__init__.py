"""메리츠 Open API — 분할 배분·수량 계산 예제.

`indicators/` 와 계약이 다르다. 지표는 시장 데이터만 받아 지표값을 내지만,
여기 모듈은 **보유 상태(예산·평단·수량·회차)를 함께 받아 금액과 수량을 낸다.**
그래서 패키지를 나눴다 — 같은 곳에 두면 지표 집계가 이들을 끌어들이고,
금액이 지표값처럼 읽힌다.

회차·비율·목표에 기본값을 두지 않는다. 함수 안에 상수로 박으면 그 숫자가
적정하다는 진술이 된다. 호출자가 정하고, 이 코드는 정해진 값을 계산만 한다.

⚠️ 산출되는 수량·금액은 **계획이지 주문이 아니다.** 어떤 모듈도 주문 API를
   호출하지 않는다. 투자자문·매매권유가 아니며, 실거래는 이용자 책임이다.
"""
