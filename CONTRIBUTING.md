# 기여 안내

메리츠증권 Open API 예제 저장소에 관심 가져 주셔서 감사합니다.
이 문서는 이슈를 올리시거나 코드를 보내실 때 참고하실 내용을 담았습니다.

## 먼저 확인해 주십시오

**계좌·주문·거래에 관한 문의는 이곳에서 처리해 드릴 수 없습니다.**

- 주문이 나가지 않았다, 잔고나 수익률이 다르게 보인다, 체결 내역을 확인하고 싶다
- 앱키 발급, 계좌 연결, 이용 신청
- API 명세·업무 규칙에 대한 문의

이런 내용은 **개발자 포털의 문의 채널**을 이용해 주십시오. 이 저장소는 예제 코드만
다루며, 저장소 관리자에게는 계좌를 조회하거나 주문 상태를 확인할 권한이 없습니다.

이곳에서 다루는 것은 **이 저장소의 예제 코드**에 관한 문제입니다.

## 자격증명과 계좌 정보

이슈·PR·커밋에 다음을 절대 넣지 말아 주십시오.

- 앱키(App Key)·시크릿(App Secret)·접근토큰
- 계좌번호
- 잔고·보유종목·주문번호 등 실제 계좌에서 나온 값

이슈는 누구나 볼 수 있고, 지우셔도 수정 이력에 남습니다.
이미 노출하셨다면 개발자 포털에서 **즉시 재발급**해 주십시오.

로그를 옮기실 때는 자격증명과 계좌번호를 `***` 로 가리고, 금액과 종목은
지우시거나 임의의 값으로 바꿔 주십시오. 예시로 쓰실 값도 실제 계좌번호나
실제 금액이 아니어야 합니다.

## 이슈

이슈 양식을 골라 주십시오. 빈 이슈는 열어 두지 않았습니다.

| 양식 | 언제 |
|---|---|
| 예제가 동작하지 않습니다 | 저장소의 코드를 실행했을 때 생긴 문제 |
| 문서·명세가 실제와 다릅니다 | README·주석의 설명이 실제 동작과 어긋날 때 |
| 이런 예제가 있었으면 합니다 | 새 예제 제안, 개선 제안 |

재현 방법과 함께 **어느 서버였는지(운영·개발)**, 호출하신 **API 경로**,
저장소 버전, 파이썬 버전을 적어 주시면 확인이 훨씬 빠릅니다.

## 개발 환경

Python 3.11 이상이 필요합니다.

```bash
git clone https://github.com/meritz-securities/open-api.git
cd open-api
pip install -e ".[dev]"
```

## 테스트

```bash
pytest tests/ -q
```

앱키나 네트워크 없이 돕니다. `tests/test_examples.py` 는 예제 파일이 문법적으로
올바른지, 확인 단계 같은 약속이 지켜지고 있는지를 봅니다.

**테스트를 위해 실제 API 를 호출하지 말아 주십시오.** 앱키에 연결된 계좌는
실계좌입니다.

## 코드를 보내실 때

이 저장소는 **라이브러리가 아니라 예제 모음**입니다. 하위 호환을 약속하는 인터페이스가
아니라 연동 방법을 보여드리는 코드이므로, 아래를 지켜 주시면 좋겠습니다.

- 한 파일에 한 주제만 담아 주십시오. 예제는 짧게 읽히는 것이 목적입니다
- 자동 재시도·백오프나 웹소켓 재연결 같은 운영 기능은 일부러 넣지 않았습니다.
  (호출 한도 준수·커넥션 재사용·토큰 재사용·로깅은 이미 들어 있습니다.)
  이런 기능을 더하시려면 먼저 이슈로 이야기를 나눠 주십시오
- 주문 예제(`examples/05_order.py`)의 **확인 단계를 지우지 말아 주십시오.**
  인자만 받고 바로 전송하는 모양으로 바꾸지 말아 주십시오
- **특정인의 저서·강의·유료 서비스에서 쓰는 고유 명칭을** 코드·문서·상태값·
  커밋 메시지에 넣지 말아 주십시오. 계산 방법은 일반 명칭으로 적어 주십시오
- **지표와 계산기의 상태값에 매매 판단 어휘를 쓰지 말아 주십시오**
  (`BUY`·`SELL`·`진입`·`익절` 등). 관찰한 사실만 서술하는 이름을 붙여 주십시오.
  `tests/` 가 이 규칙을 검사합니다
- **수익률·승률·최대낙폭을 산출하는 코드는 받지 않습니다.** 수수료·세금·
  슬리피지를 반영하지 않은 성과 수치는 그 자체로 잘못된 정보입니다
- 코드 스타일은 `pyproject.toml` 의 ruff 설정을 따릅니다 (줄 길이 120)
- 커밋 메시지와 PR 설명은 한국어로 적어 주셔도 됩니다

## 기여하시는 방법

이 저장소는 사내 저장소에서 만들어 내보내는 **스냅숏**입니다. 배포할 때마다
전체를 새로 씁니다. 그래서 **여기에 올리신 PR 은 머지하지 않습니다** —
머지해도 다음 배포에서 지워지기 때문입니다.

대신 이렇게 처리합니다.

1. 포크해서 브랜치를 만들고 변경한 뒤 `pytest tests/ -q` 를 돌려 주십시오
2. PR 을 올려 주십시오. 내용과 의도를 읽는 데 그대로 씁니다
3. 채택하면 저희가 커밋을 그대로 가져갑니다(`cherry-pick`). 저작자는
   보내 주신 그대로 남습니다
4. 다음 배포에 담아 내보내고, PR 은 반영된 커밋을 적어 닫습니다

고치실 것을 글로 적는 편이 빠르시면 **이슈만 열어 주셔도 됩니다.** 코드가
없어도 됩니다. 재현 방법과 기대한 동작을 적어 주시면 그대로 다룹니다.

`main` 에 직접 푸시하실 수 없습니다. 배포 자동화만 씁니다.

## 보안 취약점

인증 우회·자격증명 노출·권한 상승과 관련된 내용은 **공개 이슈나 PR 로 올리지 마시고**,
[SECURITY.md](SECURITY.md) 에 적힌 절차를 따라 주십시오.

## 라이선스와 권리

PR 을 보내시는 것은 아래에 동의하신다는 뜻입니다.

- 보내신 내용은 이 저장소와 같은 [MIT 라이선스](LICENSE)로 배포됩니다.
- 보내신 내용은 본인이 작성하셨거나 제출할 권한을 가지고 계십니다.
- 제3자의 코드·문서를 포함하셨다면 출처와 라이선스를 PR 에 적어 주십시오.
  MIT 와 함께 배포할 수 없는 라이선스의 코드는 받지 않습니다.
- 소속 기관이 저작권을 갖는 결과물이라면 제출 전에 그 기관의 허락을 받아 주십시오.

병합된 기여는 MIT 라이선스에 따라 메리츠증권과 다른 이용자가 이용합니다.
기여자의 저작권은 기여자에게 남습니다.
