# 면책 및 이용 조건

이 저장소는 메리츠증권 Open API 를 파이썬에서 호출하는 실행 예제입니다.
예제를 실행하시면 실제 API 가 호출되고 실제 계좌가 조회됩니다.
실행하시기 전에 반드시 읽어 주십시오.

## 투자권유가 아닙니다

API 호출을 돕는 개발 보조 수단입니다.

- 투자자문·투자권유·매매 추천을 제공하지 않습니다.
- 조회되는 시세·잔고·수익률은 참고 정보이며, 투자 판단의 근거로 삼기 위한
  정확성과 완전성을 보장하지 않습니다.
- 투자 결정과 그 결과에 대한 책임은 전적으로 이용자에게 있습니다.

## 기본값은 운영 서버입니다

`MERITZ_BASE_URL` 을 지정하지 않으시면 운영 서버로 호출됩니다.
다른 서버로 붙으시려면 `MERITZ_BASE_URL` 과 `MERITZ_WS_URL` 을 함께
지정해 주십시오.

## 계좌 연결 주의

**앱키에는 계좌가 묶여 있습니다.** 계좌번호를 입력하지 않으셔도 해당 계좌의
잔고와 자산이 조회되고, 주문도 그 계좌로 전송됩니다.

앱키에 연결된 계좌는 실계좌입니다. 모의계좌라는 전제로 사용하지 말아 주십시오.

## 접수와 체결은 다릅니다

주문 응답이 성공(`rsp_cd`=`0001`)으로 오더라도 접수되지 않았을 수 있습니다.
`data.warn_cls_code` 가 경고 값이면 `warn_cnfr_yn=A` 로 재전송하셔야 접수됩니다.
접수는 체결이 아닙니다. 최종 상태는 주문내역 조회로 확인해 주십시오.

## 주문 예제

`examples/05_order.py` 는 그대로 실행하셔도 주문이 나가지 않습니다.
미리보기까지만 진행합니다.

**이 확인 단계는 `ApiClient.call()` 에 있습니다.** 예제로 실행하시든 `meritz/`
공통 코드를 직접 호출하시든 똑같이 걸립니다. 조회 전용이 기본값이라, 주문·환전을
쓰시려면 `MERITZ_READ_ONLY=0` 으로 두셔야 합니다.

주문 함수를 "인자만 받고 바로 전송" 하는 모양으로 만들지 않은 것이 요점입니다.
이 예제를 고쳐 쓰실 때 확인 단계를 지우지 말아 주십시오.

## 예제 코드의 성격

- 라이브러리가 아닙니다. 공개된 예제와 공통 코드의 인터페이스는 별도의 하위 호환 정책을 제공하지 않습니다. 버전별 변경 사항은 저장소의 Release 정보를 확인하십시오.
- 오류 처리·재시도·동시성·호출 한도 조절은 최소한만 다룹니다.
  그대로 운영에 넣지 말아 주십시오.
- `meritz/` 는 예제 공통 코드일 뿐 배포용 SDK 가 아닙니다.

## 지표와 계산 예제

`indicators/` 와 `position_sizing/` 는 받아 온 데이터로 무엇을 계산하는지
보여드리는 예제입니다. **어느 모듈도 주문을 전송하지 않습니다.**

- 투자자문·투자권유·매매 추천이 아니며, 투자광고가 아닙니다. 특정 종목이나
  특정 계산 방식을 권하지 않습니다.
- 계산에 쓰이는 기간·기준선·회차·비율·목표는 **이용자가 지정하는 값**입니다.
  예제에 적힌 값은 계산을 보여드리기 위한 것일 뿐 권장값이 아니며,
  메리츠증권은 그 값의 적정성을 검토하지 않습니다.
- 산출값은 지표 상태와 산술 계산 결과입니다. **매수·매도 지시가 아닙니다.**
- **수익률·승률·최대낙폭을 산출하지 않습니다.** 수수료·세금·슬리피지·체결
  지연·호가 단위·유동성을 반영하지 않으므로, 이 예제로 매매 성과를 추정할 수
  없습니다.
- **분할 계산에는 손절 규칙이 없습니다.** 하락이 이어지면 예산 전액이
  투입되고, 그 뒤에도 하락하면 회수 수단이 남지 않습니다. 최대 손실은 투입
  원금 전액입니다. 평균단가가 내려가는 것은 손실이 줄어드는 것이 아닙니다.
- 레버리지·인버스 상품은 장기 보유 시 기초지수와 수익률이 크게 달라질 수
  있습니다. 어떤 종목에 적용할지, 적용할지 여부 자체를 직접 판단하십시오.

## 자격증명과 민감정보

- 앱키(App Key)와 시크릿(App Secret)의 보관 책임은 이용자에게 있습니다.
  코드·저장소·로그·대화 기록에 남기지 마십시오. 환경변수로만 다뤄 주십시오.
- 접근토큰은 디스크에 저장되지 않고 프로세스 메모리에만 존재합니다.
- 계좌 조회 결과에는 잔고·총자산·보유종목 등 민감정보가 포함됩니다.
  화면 공유나 기록이 남는 환경에서 사용하실 때 주의해 주십시오.

## 단말 식별정보 전송

게이트웨이 요구에 따라 모든 요청 헤더에 실행 단말의 MAC 주소를 넣어 전송합니다
(`mac_address` 헤더, 없으면 `IGW50024` 로 거절됩니다).
수집·이용에 관한 사항은 메리츠증권 Open API 이용약관 및 개인정보 처리방침에 따릅니다.

## 명세의 한계

- 카탈로그와 명세 파일은 개발자 포털의 공개 API 정보를 기준으로 생성됩니다. 실제 이용 시에는 개발자 포털의 최신 명세와 API별 권한을 확인하십시오.
- 응답 처리 참고자료는 알려진 API별 처리 방법을 설명하며, 모든 운영 환경과 응답 변형을 포괄하지는 않습니다.
- 응답 코드가 손상되어 오는 API 가 있습니다. 그런 응답은 데이터를 돌려드리되
  "검증되지 않음" 으로 표시합니다.

## 보증의 부인과 책임의 한계

이 저장소는 **있는 그대로(as-is)** 제공되며, 특정 목적 적합성·정확성·중단 없는
동작을 보증하지 않습니다.

관련 법령이 허용하는 범위에서, 메리츠증권은 이 코드의 이용 또는 이용 불능으로
발생한 손해에 대해 책임을 지지 않습니다. 다만 메리츠증권의 고의 또는 중대한
과실로 인한 손해, 그 밖에 법령상 배제할 수 없는 책임은 그러하지 아니합니다.

이 조항은 메리츠증권 Open API 이용약관 및 이용자와 메리츠증권 사이의 거래관계에
따라 발생하는 책임을 제한하거나 변경하지 않습니다.

---

문의는 개발자 포털을 이용해 주십시오.
